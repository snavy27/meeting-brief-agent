# Python
__pycache__/
*.pyc
.venv/
venv/

# Secrets / local env — never commit the API key, Notion token, or Google credentials
.env
*.key
service-account.json
*.service-account.json
!.env.example

# Claude Code (local, non-shared)
CLAUDE.md
.claude/settings.local.json

# Generated brief output + provenance sidecar
brief.md
*.sources.json

# Generated daily packets / PDFs (scheduled_run.py --out-dir)
out/
day-*.md
day-*.pdf

# Local verification scratch (keep verify/capture.py; ignore its generated output)
verify/briefs/
verify/*.json
