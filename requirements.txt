claude-agent-sdk>=0.2.110
python-dotenv>=1.0
# Live `--calendar` CLI only (read-only Google Calendar via a service account):
google-api-python-client>=2.0
google-auth>=2.0
# Scheduled run (scheduled_run.py): packet -> PDF, and Europe/Paris timezone data on Windows/CI.
fpdf2>=2.7
tzdata
